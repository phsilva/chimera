# the classloader imports module <clsname.lower()>: "type: VideoStreamer" in
# chimera.config lands on this package, the implementation lives beside it
from chimera.controllers.videostreamer.controller import VideoStreamer as VideoStreamer
